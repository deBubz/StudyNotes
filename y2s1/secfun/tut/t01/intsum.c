#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int first, secc, sum;
    printf("Enter the First Int: ");
    scanf("%d", &first);
    printf("Enter the Seccond Int: ");
    scanf("%d", &secc);
    printf("Total is %d", first+secc);
    return 0;
}